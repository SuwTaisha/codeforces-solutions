#include <stdio.h>

int main()
{

    int n;
    scanf("%d", &n);
    int color[200];
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &color[i]);
    }

    //i gave up this prob at this point, will comeback later very soon
    return 0;
}